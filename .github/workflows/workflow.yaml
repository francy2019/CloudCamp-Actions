name: Hello world
on:
  push:

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4.1.7
      - run: ls -la
      - name: terraform-version
        run: |
          terraform version
          echo "Ok"

      - name: terraform-init
        run: |
          terraform init

      - name: terraform-validate
        run: |
          terraform validate